node_modules
/node_modules

# build
/.next
/out
/build

# variáveis de ambiente — .env.example é versionado, o resto não
.env
.env.local
.env*.local

# testes e evidências geradas
/coverage
/playwright-report
/test-results
/blob-report
/playwright/.cache

# build do documento do PFC (gerado por `npm run pfc:build`)
/docs/pfc/build

# gerado pelo tsc
*.tsbuildinfo

# Cache local do instalador de skills (skills-lock.json).
# O conteúdo versionado fica em .claude/skills/, que é o caminho que o
# Claude Code lê — versionar os dois guardaria os mesmos bytes duas vezes.
/.agents/
